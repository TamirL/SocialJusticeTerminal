<Window x:Class="SocialJusticTerminal.Views.NewPurchaseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:SocialJusticTerminal.ViewModels"
        Title="מסוף צדק חברתי" SizeToContent="WidthAndHeight" Style="{StaticResource WindowStyle}">
    <Window.DataContext>
        <viewModels:AddPurchaseViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Grid.ColumnSpan="2">אנא הזן את מחיר הקניה אשר הלקוח ביצע:</Label>
        <TextBox Grid.Row="1" Grid.Column="0" Text="{Binding PriceString, UpdateSourceTrigger=PropertyChanged}" ToolTip="אנא הכנס מספר חיובי"/>
        <Button Grid.Row="1" Grid.Column="1" Command="{Binding AddPruchaseCommand}" ToolTip="אנא הכנס מספר חיובי">שמור קניה</Button>
    </Grid>
</Window>
